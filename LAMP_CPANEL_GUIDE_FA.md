# 📙 راهنمای جامع نصب و راه اندازی بستر LAMP (PHP + phpMyAdmin) روی cPanel

این فایل راهنمای گام‌به‌گام برای نصب کامل پلتفرم دست اول روی **هاست‌های معمولی cPanel یا DirectAdmin با Apache و PHP و phpMyAdmin** بدون نیاز به سرور مجازی یا Node.js می‌باشد.

---

## 📁 ساختار فایل‌های پروژه برای cPanel:

پروژه دارای بخش‌های آماده زیر برای هاست cPanel است:

1. **`database.sql`**: دیتابیس کامل آماده‌ی Import در phpMyAdmin.
2. **`php/config.php`**: تنظیمات اتصال به دیتابیس MySQL هاست cPanel.
3. **`php/api.php`**: کدهای ای‌پي‌آی پلتفرم به زبان PHP.
4. **`public_html_htaccess.txt`**: فایل پیکربندی آپاچی (تغییر نام به `.htaccess` پس از آپلود).

---

## 🛠️ مراحل نصب در ۳ گام ساده:

### گام اول: ساخت دیتابیس در cPanel و Import در phpMyAdmin
1. وارد cPanel شوید و روی **MySQL® Database Wizard** کلیک کنید.
2. یک دیتابیس (مثلاً `username_dastavval`) و یک کاربر دیتابیس با رمز عبور ایجاد کنید و تمام دسترسی‌ها (All Privileges) را تایید کنید.
3. از صفحه اصلی cPanel وارد ابزار **phpMyAdmin** شوید.
4. دیتابیس ساخته شده را انتخاب کرده، روی زبانه **Import** کلیک کنید و فایل `database.sql` موجود در پروژه را انتخاب کرده و دکمه **Go** را بزنید.

### گام دوم: تنظیم اطلاعات اتصال در `php/config.php`
1. فایل `php/config.php` را در cPanel باز کرده و ویرایش (Edit) کنید.
2. نام دیتابیس، نام کاربر و رمز عبور شما در این پروژه قرار داده شد:
   ```php
   $db_name = 'h353256_dast';
   $db_user = 'h353256_dst';
   $db_pass = '@Ali3360@Ali3360';
   ```

### گام سوم: آپلود فایل‌های پروژه در `public_html` یا پوشه ساب‌دامنه (Subdomain)
1. از بخش **File Manager** سی‌پنل وارد پوشه مورد نظر شوید:
   - برای **دامنه اصلی**: وارد پوشه `public_html` شوید.
   - برای **ساب‌دامنه (Subdomain)**: وارد پوشه اختصاصی ساب‌دامنه (مثلاً `public_html/sub/` یا پوشه‌ای که در بخش Subdomains تعریف کرده‌اید) شوید.
2. تمام محتویات پوشه `dist` (فایل‌های داخل dist، نه خود پوشه dist) همراه با پوشه `php/` و فایل `database.sql` را آپلود کنید.
3. فایل `.htaccess` (یا `public_html_htaccess.txt` تغییر نام یافته به `.htaccess`) را حتماً در همان پوشه قرار دهید. (دقت کنید گزینه "Show Hidden Files" در تنظیمات cPanel File Manager فعال باشد).

---

## 🌐 راهنمای اختصاصی نصب روی ساب‌دامنه (Subdomain)
سامانه دست اول به لطف معماری هوشمند آدرس‌دهی نسبی (`base: './'`) و رول‌های بدون دامنه ثابت در `.htaccess` به طور کامل با ساب‌دامنه‌ها سازگار است:
1. در cPanel وارد بخش **Subdomains** شوید و ساب‌دامنه خود را ایجاد کنید (مثلاً `app.yourdomain.com`).
2. cPanel یک Document Root برای ساب‌دامنه ایجاد می‌کند (مثلاً `/public_html/app` یا `/app`).
3. تمامی فایل‌های پروژه + فایل `.htaccess` + پوشه `php/` را مستقیماً داخل همین پوشه ساب‌دامنه قرار دهید.
4. دیتابیس `h353256_dast` و تنظیمات `php/config.php` به صورت خودکار از همین ساب‌دامنه فراخوانی می‌شوند.

---

✨ **تبریک! پلتفرم شما روی بستر LAMP (PHP + MySQL + phpMyAdmin) روی cPanel با بالاترین سرعت آماده استفاده است.**
