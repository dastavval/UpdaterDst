# راهنمای جامع کانفیگ و راه‌اندازی سامانه دست اول روی سرور و هاست شخصی

این راهنما به شما کمک می‌کند تا این اپلیکیشن فول‌استک (Express + React + Vite) را به صورت کامل و اصولی روی سرور مجازی (VPS) یا هاست‌های مجهز به Node.js کانفیگ و مستقر کنید.

---

## فهرست مطالب
۱. [نیازمندی‌های سیستم](#۱-نیازمندیهای-سیستم)
۲. [مراحل راه‌اندازی سریع روی سرور (پیش‌فرض)](#۲-مراحل-راهاندازی-سریع-روی-سرور-پیشفرض)
۳. [کانفیگ فرآیندها با PM2 (پایداری و اجرای پس‌زمینه)](#۳-کانفیگ-فرآیندها-با-pm2-پایداری-و-اجرای-پسزمینه)
۴. [تنظیمات وب‌سرور Nginx به عنوان Reverse Proxy](#۴-تنظیمات-وبسرور-nginx-به-عنوان-reverse-proxy)
۵. [پیکربندی متغیرهای محیطی (Secrets)](#۵-پیکربندی-متغیرهای-محیطی-secrets)
۶. [اتصال و انتقال دیتابیس لوکال](#۶-اتصال-و-انتقال-دیتابیس-لوکال)

---

## ۱. نیازمندی‌های سیستم
برای اجرای روان پروژه، مطمئن شوید موارد زیر روی سرور شما نصب هستند:
* **Node.js**: نسخه ۱۸ یا بالاتر (پیشنهاد ما: نسخه v20 LTS)
* **NPM**: نسخه ۹ یا بالاتر
* **PM2**: برای مدیریت پروسه و جلوگیری از خاموش شدن سرور
* **Nginx**: به عنوان سرویس‌دهنده معکوس جهت هندل کردن ترافیک HTTPS و پورت ۳۰۰۰

---

## ۲. مراحل راه‌اندازی سریع روی سرور (پیش‌فرض)

### گام اول: انتقال کدها به سرور
ابتدا کل فایل‌های پروژه (فایل ZIP یا مخزن گیت) را روی سرور آپلود کنید:
```bash
git clone <your-repository-url>
cd dastavval
```

### گام دوم: نصب پکیج‌ها و متعلقات
در پوشه اصلی پروژه، دستور زیر را برای نصب تمامی پکیج‌های پیش‌نیاز اجرا کنید:
```bash
npm install
```

### گام سوم: پیکربندی فایل محیطی `.env`
یک فایل جدید به نام `.env` ایجاد کرده و کلیدهای امنیتی خود را داخل آن قرار دهید:
```env
# کلید اختصاصی جمینی برای هوش مصنوعی مشاور و تولید کاتالوگ
GEMINI_API_KEY=your_actual_gemini_api_key_here

# محیط کاربری
NODE_ENV=production
```

### گام چهارم: بیلد گرفتن از پروژه در حالت محصول (Production Build)
برای ایجاد بیلد بهینه‌شده سمت کاربری (React) و سرور (Express cjs bundle)، دستور زیر را در خط فرمان سرور وارد کنید:
```bash
npm run build
```
این دستور کدهای وب‌سایت را فشرده کرده و پوشه خروجی `dist/` را به همراه فایل اجرایی یکپارچه `dist/server.cjs` تولید می‌کند.

---

## ۳. کانفیگ فرآیندها با PM2 (پایداری و اجرای پس‌زمینه)

برای اینکه با بستن محیط ترمینال، سایت شما از دسترس خارج نشود و در صورت کراش احتمالی خودکار ری‌استارت شود، از مدیر فرآیند **PM2** استفاده می‌کنیم:

### نصب PM2 به صورت سراسری:
```bash
sudo npm install pm2 -g
```

### استارت زدن سرویس با PM2:
در ریشه پروژه دستور زیر را وارد کنید تا وب‌سایت شما روی پورت ۳۰۰۰ اجرا شود:
```bash
pm2 start dist/server.cjs --name "dastavval-app"
```

### تنظیم PM2 برای بالا آمدن خودکار پس از ریبوت سرور:
```bash
pm2 startup
pm2 save
```

### بررسی وضعیت اجرای اپلیکیشن:
```bash
pm2 status
pm2 logs dastavval-app
```

---

## ۴. تنظیمات وب‌سرور Nginx به عنوان Reverse Proxy

برای اینکه کاربران بتوانند بدون تایپ پورت ۳۰۰۰ و با دامنه اختصاصی شما (مثال: `dastavval.com`) به سایت متصل شوند، تنظیمات Nginx را به شکل زیر انجام دهید:

۱. فایل تنظیمات پیش‌فرض دامنه در Nginx را باز کنید:
```bash
sudo nano /etc/nginx/sites-available/default
```

۲. تنظیمات مربوط به `server` را به شکل زیر ویرایش کنید:
```nginx
server {
    listen 80;
    server_name dastavval.com www.dastavval.com; # دامنه خود را بنویسید

    # فوروارد کردن ترافیک به سمت پورت ۳۰۰۰ داخلی
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

۳. صحت پیکربندی Nginx را بسنجید و آن را ری‌استارت کنید:
```bash
sudo nginx -t
sudo systemctl restart nginx
```

برای فعال‌سازی HTTPS رایگان و گواهی امنیتی SSL، دستور زیر را اجرا کنید تا پروتکل به صورت خودکار فعال شود:
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d dastavval.com -d www.dastavval.com
```

---

## ۵. پیکربندی متغیرهای محیطی (Secrets)

تمامی اطلاعات حساس مانند کلیدهای هوش مصنوعی یا کدهای مرچنت درگاه بانکی نباید در کدهای فرانت‌اند وب‌سایت قرار بگیرند. 
* سرویس‌های هوش مصنوعی (مانند مشاور خرید B2B و تولید توضیحات خودکار کاتالوگ با هوش مصنوعی) به طور کامل از طریق بک‌اند امن Express در فایل `server.ts` پروکسی شده‌اند.
* حتما مطمئن شوید `GEMINI_API_KEY` در فایل `.env` سرور تعریف شده باشد تا در پردازش‌های هوش مصنوعی اختلالی ایجاد نشود.

---

## ۶. انتقال و حفظ دیتابیس پیکربندی‌ها

برخلاف سامانه‌های پیچیده‌ای که نیاز به راه‌اندازی پردردسر دیتابیس‌های سنگین دارند، سامانه دست اول از معماری چابک مبتنی بر فایل‌ها بهره می‌گیرد:
* **تنظیمات برندینگ، نام کارخانجات و دسته‌بندی‌ها**: تماماً در فایل لوکال `/b2b-config.json` در ریشه پروژه ذخیره می‌شوند. هر تغییری که از پنل مدیریت وب‌سایت روی نام اپ، دسته‌بندی‌ها یا کارخانجات انجام دهید مستقیماً در این فایل به صورت زنده نوشته و نگهداری می‌شود.
* **محصولات کارخانه**: در فایلی مستقل به عنوان حافظه محلی ذخیره شده و نیازی به اتصالات فرعی ندارد.
* جهت پشتیبان‌گیری مرتب از اطلاعات کارخانه‌ها و تنظیمات فروشگاه خود، کافیست از فایل `b2b-config.json` کپی پشتیبان تهیه کنید.
